let getArgs = require("./helpers/args");
let startCli = () => {
  let args = getArgs(process.argv);
  console.log(args);
  if (args.h) {
  } 
   if (args.s) {
  } 
   if (args.t) {
  }
  // result
};

startCli();
